import { SortHelperPipe } from './sort-helper.pipe';

describe('SortHelperPipe', () => {
  it('create an instance', () => {
    const pipe = new SortHelperPipe();
    expect(pipe).toBeTruthy();
  });
});
