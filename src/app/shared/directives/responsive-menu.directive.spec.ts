import { ResponsiveMenuDirective } from './responsive-menu.directive';

describe('ResponsiveMenuDirective', () => {
  it('should create an instance', () => {
    // const directive = new ResponsiveMenuDirective();
    // expect(directive).toBeTruthy();
  });
});
