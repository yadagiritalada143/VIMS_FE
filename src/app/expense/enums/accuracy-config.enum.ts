export enum AccuracyConfigTypes {
  AMOUNT = 'amount',
  AMOUNT_PERCENTAGE = 'amount_percentage',
  TAX = 'tax',
  TAX_PERCENTAGE = 'tax_percentage',
  FEE = 'fee',
  FEE_PERCENTAGE = 'fee_percentage',
}

export enum CurrencyConfig {
  SYMBOL = 'symbol',
  DEFAULT_CURRENCY = 'USD',
}
